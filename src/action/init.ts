/**
 * @author WMXPY
 * @namespace Action
 * @description Init
 */

import { BarkTemplate } from "../config/declare";
import { Environment } from "../config/environment";

export const initAction = async (env: Environment, query: BarkTemplate, targetPath: string): Promise<void> => {

    return;
};
