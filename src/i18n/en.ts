/**
 * @author WMXPY
 * @namespace Internationalization
 * @description En
 */

import { TEXT } from "./declare";

export const EN_US_TEXT: Record<TEXT, string> = {

    [TEXT.PACKAGE_ALREADY_INSTALLED_REPLACE]: "Package already installed, replacing...",
    [TEXT.PACKAGE_ALREADY_INSTALLED_ABORT]: "Package already installed",
};
